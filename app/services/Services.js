/**
 * @ngdoc controller
 * @name services
 *
 * @description
 * A module that contains all the reusable services. 
 * Defining it like this makes it easier to share it between projects.
 *
 */
'use strict';

var services = angular.module('services', []);