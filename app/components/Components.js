/**
 * @ngdoc controller
 * @name components
 *
 * @description
 * A module that contains all the reusable components (e.g. a set of controls with label and help button). 
 * Defining it like this makes it easier to share it between projects.
 *
 */
'use strict';

var components = angular.module('components', []);